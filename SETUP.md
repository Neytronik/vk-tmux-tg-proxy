# 🔧 Полная инструкция по настройке VK Tmux Bot

## Шаг 0: Что понадобится

- Сервер/компьютер с Linux, tmux и Python 3
- Аккаунт ВКонтакте
- Телефон с VK (для проверки)

---

## Шаг 1: Создание группы ВКонтакте

1. Зайди на [vk.com/groups](https://vk.com/groups) с компьютера
2. Нажми **«Создать сообщество»**
3. Выбери тип: **«Группа по интересам»** (или любой другой)
4. Придумай название (например, «Мой сервер»)
5. **Важно:** поставь галочку **«Закрытая группа»** — чтобы никто кроме тебя не видел сообщения
6. Нажми «Создать»

---

## Шаг 2: Получение токена (ключа доступа)

1. В группе перейди: **Управление → Настройки → Работа с API**
2. Нажми **«Создать ключ»**
3. В открывшемся окне:
   - Поставь галочки: **«Сообщения сообщества»**, **«Разрешить приложению доступ к сообщениям»**
   - Остальные галочки можно снять (они не нужны)
4. Нажми **«Создать»**
5. **Скопируй токен!** Он выглядит так:
   ```
   vk1.a.A1b2C3d4E5f6G7h8I9j0K1l2M3n4O5p6Q7r8S9t0U1v2W3x4Y5z6...
   ```
   ⚠️ Токен показывается **только один раз**! Не потеряй.

---

## Шаг 3: Включение Long Poll API

Всё там же: **Управление → Настройки → Работа с API → Long Poll API**

1. Поставь переключатель **«Long Poll API» → Включено**
2. В блоке **«Типы событий»** отметь галочку:
   - ✅ **Входящие сообщения**
3. Остальные галочки можно снять
4. **Версия API:** оставь 5.199 (или новее)
5. Нажми **«Сохранить»**

---

## Шаг 4: Включение бота в сообщениях

1. Перейди: **Управление → Настройки → Сообщения**
2. Включи **«Сообщения сообщества»**
3. В блоке **«Настройки для бота»**:
   - Включи **«Возможности ботов»**
   - Нажми **«Начать»** (кнопка для первого сообщения)
   - **«Возможности ботов» должны быть ВКЛ**

---

## Шаг 5: Получение ID группы

1. На главной странице группы посмотри на адресную строку браузера:
   ```
   https://vk.com/club123456789
   ```
2. **`123456789`** — это ID группы. Запомни его.
3. Если в адресе буквенный адрес (например, `myserver`), то:
   - Перейди в **Управление → Настройки → Адрес**
   - Там будет поле «Номер» — это и есть ID

---

## Шаг 6: Получение твоего VK ID

Есть несколько способов:

**Способ 1 (простой):**
1. Зайди на [regvk.com/id/](https://regvk.com/id/)
2. Вставь ссылку на свою страницу VK
3. Скопируй «ID пользователя» (число)

**Способ 2 (через настройки):**
1. Открой VK → Настройки → Аккаунт
2. Прокрути вниз до «Адрес страницы»
3. Если там число — это твой ID
4. Если буквы — скопируй этот адрес и узнай ID через способ 1

**Способ 3 (для технарей):**
1. Открой любую фотографию на своей странице
2. Посмотри на URL: `photo123456789_457239017`
3. Число до `_` — это твой ID

---

## Шаг 7: Настройка бота (на сервере)

```bash
cd /home/vkbot

# Интерактивная настройка
.venv/bin/python run.py init
```

Затем отвечаешь на вопросы:
```
Токен группы: vk1.a.A1b2... (вставляешь токен из Шага 2)
ID группы: 123456789           (из Шага 5)
Разрешённые ID: 987654321      (твой ID из Шага 6)
```

---

## Шаг 8: Запуск бота

```bash
# Проверить, что всё настроено
.venv/bin/python run.py status

# Запустить вручную (для проверки)
.venv/bin/python run.py start

# Или установить как сервис (автозапуск после перезагрузки)
sudo cp vk-tmux-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable vk-tmux-bot
sudo systemctl start vk-tmux-bot

# Посмотреть логи
journalctl -u vk-tmux-bot -f
```

---

## Шаг 9: Первое сообщение боту

1. Открой VK на телефоне или компьютере
2. Зайди в свою группу
3. Нажми **«Сообщения»** (или «Написать сообщение»)
4. Напиши **`/help`**
5. Бот должен ответить со списком команд!
6. Если не отвечает — проверь логи: `journalctl -u vk-tmux-bot -n 50`

---

## 🔍 Проверка (troubleshooting)

### Бот не отвечает на сообщения

```bash
# 1. Проверь статус
.venv/bin/python run.py status

# 2. Проверь токен (прямой запрос к API)
curl -s "https://api.vk.com/method/groups.getById?v=5.199&access_token=ТВОЙ_ТОКЕН" | python3 -m json.tool

# 3. Посмотри логи сервиса
sudo journalctl -u vk-tmux-bot -n 100
```

### Ошибка «invalid access_token»
- Проверь, не истёк ли токен
- Пересоздай токен в настройках группы (Шаг 2)

### Ошибка «Permission denied» при работе с tmux
- Убедись, что tmux сервер запущен от твоего пользователя:
  ```bash
  tmux new-session -d -s test && tmux kill-session -t test
  ```
- Бот использует сокет `/tmp/tmux-$(id -u)/default`

---

## 🎯 Быстрая проверка всего

```bash
# Запусти все тесты (должно быть 35/35 OK)
cd /home/vkbot && .venv/bin/python tests.py
```
